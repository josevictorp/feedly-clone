/**
 * Root component.
 *
 * The foundation milestone ships a blank page on purpose: the shell, sidebar
 * and streams arrive in M4 and M5, built against the captured Feedly UI.
 */
export function App() {
  return <div id="app" data-testid="app-root" />
}
