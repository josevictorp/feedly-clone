{
  "name": "@feedly/server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Local Feedly clone server: API, feed scheduler and static hosting.",
  "main": "./dist/index.js",
  "scripts": {
    "build": "tsc --build",
    "dev": "node --watch src/index.ts"
  },
  "dependencies": {
    "@feedly/shared": "workspace:*",
    "@hono/node-server": "^2.1.1",
    "hono": "^4.13.8",
    "pino": "^10.0.0"
  }
}
