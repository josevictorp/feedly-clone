/**
 * Application version.
 *
 * Kept as a literal instead of importing package.json so the value survives
 * both the TypeScript build and Node's type stripping without a JSON loader.
 */
export const APP_VERSION = '0.1.0'
